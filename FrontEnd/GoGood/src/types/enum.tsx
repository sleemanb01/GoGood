export enum USTATUS { //upload status
  UNINIT,
  PENDING,
  SUCCESS,
  FAILED,
}

export enum PSTATUS {
  CLOSED,
  SAY_THANKS,
  PENDING,
  SHOW_PROPOSERS,
  WAITING_FOR_DATE,
  IS_DATE_OK,
  IN_HANDLE,
}

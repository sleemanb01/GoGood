export enum USTATUS { //upload status
  UNINIT,
  PENDING,
  SUCCESS,
  FAILED,
}

export enum PSTATUS {
  HANDLED,
  PENDING,
  ACCEPT_HELP,
  WAITING_FOR_DATE,
  IS_DATE_OK,
  CHOOSE_ANOTHER_DATE,
  IN_HANDLE,
}
